# arkapp-ark
AI-native terminal and SSH client for macOS and Windows. Terminal, SFTP file transfer, and live server state share one connection, so the built-in AI agent works from real context instead of what you describe. Free plan available.
